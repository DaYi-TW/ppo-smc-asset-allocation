# Vite 開發/測試環境預設值（committed）。
# 本地若要改接真實 backend，建一個 .env.local（gitignored）覆寫即可。
#
# 必要：e2e 測試靠 MSW 攔截 /api/v1/*；CI 沒有 .env.local，靠這份。
VITE_USE_MOCK=true

# 預設後端位址（mock 模式不真正會 fetch，但 client 仍需要 base URL 才不會 throw）
VITE_API_BASE_URL=http://localhost:8080
VITE_SSE_BASE_URL=http://localhost:8080
