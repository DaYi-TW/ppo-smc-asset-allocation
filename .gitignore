# --------------------------------------------------------------------------
# Secrets — MUST never be committed
# --------------------------------------------------------------------------
.env
.env.*
!.env.example
# warroom 前端 dev/test 靠 VITE_USE_MOCK=true（MSW）。
# CI 無 .env.local，靠這份 committed 的 .env.development 載入 mock 設定（無秘密）。
!apps/warroom/.env.development
*.key
*.pem
*.p12
secrets/
credentials.json

# --------------------------------------------------------------------------
# Python
# --------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/
.python-version

# Testing & coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Type checkers
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/

# Linters
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# --------------------------------------------------------------------------
# Data ingestion (002) — staging dirs are intermediate, never commit
# --------------------------------------------------------------------------
data/raw/.staging-*/
data/raw/.tmp/
# 010: local archive of old snapshots when refreshing data window (Constitution I restore-path)
data/raw_archive_*/

# --------------------------------------------------------------------------
# SMC Feature Engine (001) — visualisation output (PNG / HTML preview files)
# --------------------------------------------------------------------------
reports/

# --------------------------------------------------------------------------
# IDE / OS
# --------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# --------------------------------------------------------------------------
# Build artifacts (none yet, but reserved)
# --------------------------------------------------------------------------
*.log
*.pid

# --------------------------------------------------------------------------
# 004 PPO training run artefacts — local 訓練輸出（policy.zip / metrics.csv /
# tensorboard event files），不入 repo（依憲法 IV 微服務解耦：runs/ 為訓練端
# 私有檔案，005 推理服務透過 final_policy.zip 標準 sb3 格式消費）。
# --------------------------------------------------------------------------
runs/
