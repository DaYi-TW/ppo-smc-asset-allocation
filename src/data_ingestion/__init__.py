"""Data ingestion package — feature 002.

Public API will be re-exported here once Phase 2 / Phase 3 of tasks.md lands.
For now this is an empty package so that `pip install -e .` succeeds and the
Docker image can build before any real source code exists.
"""

__version__ = "0.1.0"
